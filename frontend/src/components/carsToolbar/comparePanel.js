import React from "react";

const ComparePanel = () => {
  const panelStyle = {
    border: "1px solid grey",
  };

  return (
    <div style={panelStyle}>
      <h2>Compare Panel</h2>
    </div>
  );
};

export default ComparePanel;
